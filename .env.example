# Arquivo: .env (raiz do projeto)
# IMPORTANTE: este é apenas o template. Copie como .env e preencha com sua chave real.
# NUNCA commite o arquivo .env — ele está no .gitignore.
OLLAMA_API_KEY=sua_chave_aqui_sem_aspas
